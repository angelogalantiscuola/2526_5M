# Soluzione verifica di recupero

File inclusi:

- `soluzione_er.mmd` — soluzione del primo esercizio (modello ER)
- `soluzione.sql` — soluzione del secondo esercizio (SQL)

Le soluzioni sono state scritte in modo coerente con il livello delle verifiche di primo quadrimestre.
