# Verifica di Informatica

## Istruzioni
- Rispondi con chiarezza e in modo esaustivo.

---

## 1) Domanda aperta: Python + Ambiente

Spiega in modo semplice:
1. Cosa significa che Python ha tipizzazione dinamica e differenza con tipizzazione statica.
2. Che cosa sono i type hints e perché li usiamo anche se l’interprete non li richiede.
3. A cosa serve `venv` e perché usiamo `requirements.txt`.
4. Perché `.gitignore` è fondamentale e quali file NON devono finire su GitHub.

---

## 2) Domanda aperta: Modellazione ER + Normalizzazione

In una risposta integrata:
1. Elenca i componenti fondamentali del modello ER (entità, attributi, relazioni, cardinalità).
2. Spiega 1:1, 1:N e N:N con un esempio pratico (es. studenti/corsi/registrazioni).
3. Mostra la trasformazione N:N in tabelle SQL (tabella di raccordo con PK e FK).
4. Definisci normalizzazione e differenza tra 1NF, 2NF, 3NF (e un’anomalia che risolve ognuna, breve).

---

## 3) Domanda aperta: SQL

Scrivi una risposta con:
1. DDL vs DML (con esempi `CREATE`, `DROP`, `INSERT`, `SELECT`).
2. Cosa fa `GROUP BY` e `HAVING`.
3. Differenza `JOIN` vs `LEFT JOIN`.
4. Cos’è la SQL injection.

